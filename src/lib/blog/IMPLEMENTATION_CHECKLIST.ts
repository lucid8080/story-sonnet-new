/**
 * Blog + Keyword Bank — implementation checklist (v1)
 *
 * - [x] Prisma models, indexes, migration SQL
 * - [x] Public visibility helper (scheduled + published) without cron
 * - [x] Admin APIs (CRUD, duplicate, publish/schedule/unpublish, AI, keywords)
 * - [x] Upload: assetKind blog_cover | blog_inline + blogSlug
 * - [x] Admin UI: list, editor (TipTap), AI panel, keyword bank
 * - [x] Public /blog, /blog/[slug], category/tag redirects, SEO, RSS, sitemap
 */

export {};
