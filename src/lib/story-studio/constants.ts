/** Max characters per episode when the LLM generates a script (~5 min narration). */
export const STORY_STUDIO_LLM_MAX_SCRIPT_CHARS_PER_EPISODE = 4950;

/** Max characters per episode for manual edit, import, and save (Story Studio). */
export const STORY_STUDIO_MAX_SCRIPT_CHARS_PER_EPISODE = 12_000;
